"""tablero-cl — comparador de precios de juegos de mesa en Chile.

Packaged so `pip install -e .` exposes the `tablero` console command (see
pyproject.toml).  The modules also run directly from inside scripts/
(`python main.py ...`); cross-module imports try the package-relative form
first and fall back to the flat form.
"""

__version__ = "0.1.0"
